// Geração de novas ceções
/*var contador = 3
function gerarNova() {
    identificador = 'postagem' + contador
    caixa = document.createElement('div')
    caixa.id = identificador
    caixa.onclick = function () {
        gerarNova()
    };
    document.querySelector('#bd').appendChild(caixa)
    contador++
}*/

// Possível método para carregar novas
/*document.addEventListener('scroll', function() {
    controle += 1
    
    if (controle%3 == 0) alert()
})*/